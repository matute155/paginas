# Base de datos PostgreSQL en Neon
# Obtén esta URL desde tu dashboard de Neon
DATABASE_URL=postgresql://username:password@hostname:5432/database?sslmode=require

# Alternativamente, puedes usar POSTGRES_URL (Neon también provee esta)
# POSTGRES_URL=postgresql://username:password@hostname:5432/database?sslmode=require

# Variables separadas (opcional, solo si no usas DATABASE_URL)
# PGUSER=your_username
# PGPASSWORD=your_password  
# PGHOST=your_hostname
# PGPORT=5432
# PGDATABASE=your_database

# Configuración de entorno
NODE_ENV=production